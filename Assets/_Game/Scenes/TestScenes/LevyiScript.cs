using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevyiScript : MonoBehaviour
{
    private float _field;
    // Start is called before the first frame update
    void Start()
    {
        // Comment
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
